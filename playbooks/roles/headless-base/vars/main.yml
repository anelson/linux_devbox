---
# vars file for roles/headless-base
utility_packages:
    - btrfs-progs
    - gnupg
    - the_silver_searcher
    - ripgrep # supposedly even faster than ag
    - fzf # file finder
    - tmux
    - tmuxp
    - zsh
    - bash-completion
    - networkmanager
    - wget
    - curl
    - p7zip
    - unzip
    - unrar
    - pass
    - bluez
    - bluez-utils
    - pulseaudio
    - pulseaudio-bluetooth
    - pamixer
    - elinks
    - libnotify

dev_packages:
    - neovim
    - python-neovim
    - python2-neovim
    - ranger
    - base-devel
    - git
    - git-lfs
    - python
    - python-pip
    - python-setuptools
    - python2
    - python2-pip
    - python2-setuptools
    - jdk8-openjdk
    - jdk9-openjdk
    - scala
    - ruby
      # dependencies for youcompleteme viM plugin
    - [ boost, boost-libs, clang, mono, ncurses5-compat-libs, nodejs, rust, cargo, cmake, make, go, npm ]

# packages that apply specifically to laptops
laptop_packages:
    - wpa_supplicant
    - powertop
    - ethtool #for disable WoL
    - tlp
    - systemd-boot-pacman-hook # make sure the systemd-boot binaries on /boot are in sync with the latest systemd

console_font_packages:
    - terminus-font
    - adobe-source-code-pro-fonts
    - system-san-francisco-font-git
    - ttf-font-awesome-4
    - powerline-fonts

